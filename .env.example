# Base URL of the jesty-backend API (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:5000/api
# Same origin as above, without /api — used for the Socket.IO client
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
